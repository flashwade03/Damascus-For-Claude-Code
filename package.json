{
  "name": "damascus",
  "version": "3.0.0",
  "description": "Workflow that forges documents through iterative multi-LLM review",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
